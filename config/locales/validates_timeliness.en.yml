en:
  errors:
    messages:
      invalid_date: "no es una fecha válida"
      invalid_time: "no es una hora válida"
      invalid_datetime: "fecha/hora inválidos"
      is_at: "debe ser a las %{restriction}"
      before: "debe ser antes que %{restriction}"
      on_or_before: "debe ser antes o igual que %{restriction}"
      after: "debe ser depués de %{restriction}"
      on_or_after: "debe ser después o igual que %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%Y-%m-%d'
      time: '%H:%M:%S'
      datetime: '%Y-%m-%d %H:%M:%S'
